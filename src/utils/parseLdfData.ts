import ldf from './json/gym_progress.json';

/**
 * Using a manually downloaded JSON file, collate gym progress into a resuable interface.
 * 
 * NOTE: Should we duplicate the board into a personal board that can be copied so we can make 
 *       direct API calls from 
 * 
 */
